#
# Tests that ClrStack properly displays async Main method names with angle brackets when DML is enabled
#

CONTINUE

LOADSOS

# Verify that ClrStack with /d (DML enabled) properly escapes async method name with angle brackets
# When DML is enabled, angle brackets should be escaped as &lt; and &gt;
SOSCOMMAND:ClrStack /d
VERIFY:.*OS Thread Id:\s+0x<HEXVAL>\s+.*
VERIFY:\s+Child\s+SP\s+IP\s+Call Site\s+
# Verify that the method name contains escaped angle brackets (&lt;Main&gt;) not literal angle brackets
VERIFY:.*AsyncMainTest\.&lt;Main&gt;\(.*\).*
